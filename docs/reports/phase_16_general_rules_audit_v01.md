# Phase 16 · 全武将规则审计 v01

> 结论先说：**本轮审计尚未覆盖整个现有武将池。** 已完成的是"全量实跑 +
> 高风险类别专项"，剩余的是"每个技能的正反场景逐一构造"。没做完的部分在
> 文末逐条列出，没有含糊过去。

- 基线：`main @ e7954ea`
- 审计方式：真实引擎实跑（不读代码猜结论），工具放在 `.cache/general_audit/`
  （`.cache/` 已 ignore，不进版本库）
- 规则来源：本项目 `SkillDef.description` 所对应的官方版本 + 官方技能文本；
  不把"最新版技能"套到旧版实现上（详见 §6 版本判定）

---

## 1. 枚举结果

从真实注册表自动枚举（`GeneralRegistry.list_generals()` × `Game.general_available`）：

| 项 | 数量 |
| --- | --- |
| 注册表里的武将 | 91 |
| **当前实际可选**（可进随机池 / 选将界面） | **84** |
| 可选武将的技能定义 | 154 |
| 技能定义缺失 | 0 |

按扩展包分布（可选）：

| 扩展包 | 数量 |
| --- | --- |
| 标准版 | 25 |
| SP | 10 |
| 风 | 10 |
| 林 | 8 |
| 火 | 8 |
| 神 | 7 |
| 山 | 5 |
| 一将成名 | 11 |

同名不同版本各自独立计入（标准关羽 / 神关羽、标准司马懿 / 神司马懿、
`zhangjiao` / `zhangjiao_2010` 等）。

---

## 2. 审计覆盖与结果

| 审计层 | 范围 | 结果 |
| --- | --- | --- |
| 全量跑局 | 84 名武将各打一局全 AI 对局 | **84/84 跑完**（0 卡死 / 0 报错）|
| 判定 / 改判专项 | 延时锦囊、八卦阵、鬼才、鬼道、天妒、屯田、刚烈、铁骑 | **17/17**（洛神已移入 Phase 16.1 的专项，见 §11）|
| View-As / 转化专项 | 自动枚举 16 个带 `conversions` 的技能 | **86/86** |
| 主动技素材专项 | 眩惑、明策、直谏、举荐 | **27/27** |
| 双雄专项（单机） | A–F 全部场景 | **33/33** |
| 双雄专项（LAN） | 真实 TCP + 真实客户端牌桌 | **10/10** |
| P0 判定优先（引擎） | 闸门、改判窗口、异常路径 | **25/25** |
| P0 判定优先（UI 层） | 真实 Renderer + `handle_game_click` | **11/11** |
| P0 判定优先（真实 main.py） | 乐不思蜀 / 八卦阵 / 鬼才 | **24/24** |

复现命令：

```powershell
.venv\Scripts\python.exe .cache/general_audit\audit_generals.py
.venv\Scripts\python.exe .cache\general_audit\audit_p2_judge.py
.venv\Scripts\python.exe .cache\general_audit\audit_p2_view_as.py
.venv\Scripts\python.exe .cache\general_audit\audit_p2_active_materials.py
.venv\Scripts\python.exe .cache\general_audit\audit_p1_shuangxiong.py
.venv\Scripts\python.exe .cache\general_audit\audit_p1_shuangxiong_lan.py
.venv\Scripts\python.exe .cache\general_audit\audit_p0_judge_priority.py
.venv\Scripts\python.exe .cache\general_audit\audit_p0_judge_ui.py

# 真实 main.py（判定优先级的手玩路径）
$env:PYTHONPATH=".cache\general_audit"; $env:SGS_RUNTIME_SCRIPT="judge_probe"
.venv\Scripts\python.exe main.py
```

---

## 3. P0 · 判定流程的全场最高优先级

### 3.1 问题

判定在实现里只是一条普通的 `Flow`，没有任何"占场"语义。结果：

- 判定结算完成后、判定牌还挂在屏幕中央时，`game.local_can_play()` 已经
  返回 True——玩家可以提前出下一张牌，于是**另一条动作链与判定的收尾同时跑**；
- 引擎的 `GameEngine.submit` 不区分"现在是不是判定中"，`UseCardAction`
  直接创建 `UseCardFlow`，任何绕过界面的提交（LAN 客户端、脚本）都能抢先出牌；
- UI 各层（按钮可用性、点击路由、技能栏）各自判断，没有统一判据。

### 3.2 方案：一个闸门，两个消费点

新增 `src/game/judge_gate.py`。它把三个必须分开的状态显式建模：

```text
JudgeFlow logical pending      规则上判定还没结束（JudgeFlow 还在跑）
Judge replacement pending      判定正停在改判窗口上等某个人出牌
Judge UI presentation active   规则上判定已经结算完，界面还在演
```

前两个由规则层回答（活跃 JudgeFlow 的注册表 + 当前 `PendingRequest`），
第三个只有表现层知道，由 `Effects.sync_judge_gate()` 每帧汇报。

消费点只有两个，规则层不再散落 `if`：

| 入口 | 作用 |
| --- | --- |
| `JudgeGate.accepts(action)` | 引擎侧权威判据：这个动作是不是"当前判定请求要的回答" |
| `JudgeGate.allows_local_input(player)` | UI 侧权威判据：本机玩家现在能不能操作牌桌 |

### 3.3 落地位置

| 文件 | 改动 |
| --- | --- |
| `src/game/judge_gate.py` | 新增；闸门本身 |
| `src/game/engine/runtime.py` | `GameEngine.submit` 进门先问闸门，被拦时返回"已取消"（不抛异常：AI 的推进路径里有几条靠 `cancelled` 才能接回回合）|
| `src/game/flows/judge.py` | `JudgeFlow._draw` 登记占用，收尾 / 取消 / 抽空牌堆时释放 |
| `src/game/core.py` | 创建闸门；`local_can_play` 先问闸门 |
| `src/game/turn.py` | `end_phase_blockers` 加判定门控 |
| `src/ui/interaction.py` | `handle_game_click` 最前面问闸门，判定期间吞掉一切普通点击 |
| `src/renderer.py` | `hit_action` 同源门控（判定面板捕获牌桌点击）|
| `src/ui/fx.py` | `sync_judge_gate`：把"判定还在演"汇报给闸门 |
| `src/ui/judge.py` | `MAX_HOLD` 兜底改为询问引擎（改判窗口开着时不再自行收面板）|
| `src/ui/view_adapter.py` | 客户端只读视图也挂闸门（房主推进了、判定牌还在这台机器上演时不放行）|

### 3.4 为什么 presentation 阶段不拦引擎

规则上判定已经结束，AI 的后续结算**必须**能继续——拦了会把 AI 的回合卡死
（AI 的推进路径不是每一条都会在失败后重试）。这一阶段要保证的只是"玩家别在
判定牌还在屏幕中央时提前出下一张牌"，所以它只作用于真人输入；动作队列那一层
的压制另由 `JudgePanel.holds_actions` 负责。

### 3.5 不会锁死

闸门按 `JudgeFlow` 的状态惰性剔除：`_LIVE_STATUS` 只含 `running` / `waiting`，
构造出来但没 `start` 的流程不占用；判定牌抽空（`_complete_with(None)`）走
`on_settled` 兜底释放；被取消的流程状态天然不在活跃集里。三条异常路径都有
用例覆盖（`audit_p0_judge_priority.py` 的最后四项）。

### 3.6 真实验证

`judge_probe.py` 挂在**真实主循环**上（`SGS_RUNTIME_SCRIPT` 机制），
screen / game / renderer 全部是玩家正常启动时正在用的那一个对象：

```text
[PASS] lebu：判定面板 active / 闸门占用中 presentation
[PASS] lebu：点手牌不产生任何动作        · handle_game_click → False
[PASS] lebu：点「结束回合」无动作        · hit_action → None
[PASS] lebu：点技能栏无动作              · hit_action → None
[PASS] lebu：点对手座位无动作            · hit_action → None
[PASS] bagua：同上（6 项）
[PASS] guicai：闸门占用中 replacement
[PASS] guicai：改判窗口期间出牌仍被拒
[PASS] guicai：改判窗口里点手牌完成的是改判（不是出牌）
[PASS] guicai：改判窗口里按钮 / 技能栏 / 座位全部无动作
```

---

## 4. 逐技能审计表

`Rule Source` 一列写的是"以哪个官方版本为准"；`Result` 只写
`PASS` / `FIXED` / `BLOCKED` / `AMBIGUOUS`，没有真实验证的不写 PASS。

### 4.1 判定 / 改判类

| Expansion | General | Skill | Rule Source | Result | Bug | Fix | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 火 | 颜良&文丑 | 双雄 | 经典版（颜色，非花色）| FIXED | 判定牌进弃牌堆而非归自己；真人选择被自动替代；无法多次转化 | `JudgeFlow.card_recipient` + 改走 `CardConversion` 视为技 | audit_p1_shuangxiong 33/33、LAN 10/10 |
| 标准版 | 司马懿 | 鬼才 | 经典版 | PASS | — | 改判走通用 `JudgeFlow` 窗口 | audit_p2_judge 17/17、main.py 探针 |
| 风 | 张角 | 鬼道 | 经典版 | PASS | — | 同上 | audit_p2_judge |
| 标准版 | 郭嘉 | 天妒 | 经典版 | PASS | — | `JUDGE_FINISHED` 取最终判定牌 | audit_p2_judge |
| 标准版 | 甄姬 | 洛神 | **官方 OL / 移动版标准版**（可重复）| FIXED | ① 缺"你可以"（到点自动判定）；② 判定牌"先弃后拿"；③ **只判定一次，丢了"然后你可以重复此流程"**（Phase 16 原报告的版本判断本身错了）| 新增 `LuoshenFlow`：确认 → 判定 → 黑色获得 → **再问是否继续** → 循环；`card_recipient` 直取 | audit_p2_luoshen 30/30 |
| 标准版 | 夏侯惇 | 刚烈 | **官方标准版**（伤害来源选择一项）| FIXED | 少了"伤害来源选择一项"：实现直接扣 1 点伤害，**漏掉"弃置两张手牌"这个选项**，也没把选择权交给伤害来源 | 新增 `GanglieFlow`：判定非红桃后再问伤害来源选一项；手牌不足两张按官方 FAQ 只能承受伤害 | audit_p2_judge + 三种情形实跑 |
| 标准版 | 马超 | 铁骑 | 经典版 | PASS | — | — | audit_p2_judge（红色判定后目标无法出闪）|
| 山 | 邓艾 | 屯田 | 经典版 | PASS | — | — | audit_p2_judge |
| 林 | 蔡文姬 | 悲歌 | 经典版 | PASS | — | 判定结果语义与花色对应 | audit_generals 跑局 |
| 林 | 董卓 | 暴虐 | 经典版 | PASS | — | — | audit_generals 跑局 |
| 神 | 神关羽 | 武魂 | 经典版 | PASS | — | — | audit_generals 跑局 |
| — | 延时锦囊 | 乐不思蜀 / 兵粮寸断 / 闪电 | 官方 | PASS | — | — | audit_p2_judge + main.py 探针 |
| — | 装备 | 八卦阵 | 官方（"你可以"）| PASS | — | 确认窗口 + 判定 | main.py 探针 |

### 4.2 View-As / 转化技（自动枚举 16 个技能）

| General | Skill | Result | 说明 |
| --- | --- | --- | --- |
| 关羽 | 武圣 | FIXED | 红色【杀】当【杀】曾被列为合法素材但提交不了（去重池缺正常使用）|
| 赵云 | 龙胆 | PASS | 双向转化；素材按使用牌生命周期离手 |
| 神赵云 | 龙魂 | PASS | X 张**同花色**；`count_for` 与 `matches` 一致 |
| 甘宁 | 奇袭 | PASS | 黑色牌当过河拆桥；目标无牌时给出明确理由 |
| 华佗 | 急救 | PASS | `available` 限定回合外（回合内查不到候选是正确行为）|
| 卧龙诸葛 | 火计 | PASS | 红色手牌当火攻 |
| 高顺 | 禁酒 | PASS | 【酒】当【杀】 |
| 董卓 | 酒池 | PASS | 无目标转化自动提交 |
| 神关羽 | 武神 | PASS | 红桃当【杀】 |
| 颜良&文丑 | 双雄 | PASS | 见 §4.1 |
| 其余 6 个 | — | PASS | 见 `audit_p2_view_as.py` 明细 |

### 4.3 真人选牌类（"程序替玩家做决定"）

| General | Skill | Result | Fix |
| --- | --- | --- | --- |
| 法正 | 眩惑 | FIXED | 删掉 `hearts[:1]` 兜底；`cost_candidates` 限定红桃手牌，`keep_cards` 让牌的去向由技能自己决定 |
| 陈宫 | 明策 | FIXED | 同上（候选：装备牌或【杀】）|
| 张昭&张紘 | 直谏 | FIXED | 同上（候选：装备牌；牌进对方**装备区**而非手牌）|
| 徐庶 | 举荐 | FIXED | 删掉自动取首；`max_cost_cards=3` 表达"至多三张"|
| 颜良&文丑 | 双雄 | FIXED | 见 §4.1 |
| 其余需要选牌的主动技 | — | AMBIGUOUS | 逐条静态复核过（见 §5），但没有逐个构造真人场景 |

### 4.4 阶段替代 / 触发技

| General | Skill | Result | Bug | Fix |
| --- | --- | --- | --- | --- |
| 张辽 | 突袭 | FIXED | 阶段替代的结果读取错误（对 `FlowResult` 调 `.get()`），实际从未跳过摸牌阶段 | 顺着完成的 `value` 取 |
| 鲁肃 | 好施 | FIXED | `on_complete` 从未被触发，替代之后回合永久停在摸牌阶段 | `Flow.notify_on_complete` 统一幂等入口 |
| 法正 | 恩怨 | FIXED | 模块级 monkey patch 自递归（`EnyuanFlow.advance` 指向调用自己的函数），伤害结算直接 `RecursionError` | 逻辑并回类定义，patch 删除 |

---

## 5. 错误类型统计（本轮实际发现）

| 错误类型 | 次数 | 涉及 |
| --- | --- | --- |
| 判定牌去向错误 | 2 | 双雄（弃牌堆而非归己）、洛神（先弃后拿）|
| 玩家选择被自动替代 | 5 | 双雄、眩惑、明策、直谏、举荐 |
| 发动时机错误（缺"你可以"）| 1 | 洛神 |
| **规则内容缺失**（少一个分支 / 少一个选项）| 2 | 洛神（少"可重复"）、刚烈（少"弃两张手牌"）——**均由错误 SkillDef 掩盖**，见 §11 |
| 次数 / ResetScope 错误 | 0 | — |
| 目标错误 | 0 | — |
| 距离错误 | 0 | — |
| View-As 错误（界面与引擎不一致）| 1 | 武圣（候选与提交判据不同源）|
| 多人顺序错误 | 0 | — |
| LAN 不一致 | 0 | 双雄的 Host 侧校验已用真实 TCP 验证 |
| AI 不一致 | 0 | AI 与真人读同一份候选（本次修正后）|
| UI 无入口 | 0 | — |
| 规则版本错误 | 0 | 见 §6 |
| 卡牌 ownership 错误 | 0 | 84 局全量跑局 + 牌唯一归属检查未报 |
| **Flow 卡死 / 递归** | **3** | 恩怨（递归）、好施（卡死）、突袭（功能失效）|
| 判定优先级缺失（架构性）| 1 | 全项目；见 §3 |

**问题最集中的地方**：`Flow` 生命周期（3 次卡死 / 递归，全部与被复用流程的
收尾回调有关）、以及"技能需要玩家选一张牌，实现却自动替他选"（5 次）。

---

## 6. 规则版本判定

审计时严格按本项目 `SkillDef.description` 与实现判断**它实现的是哪一版**，
再去找对应的官方文本；没有把"最新版技能"偷偷升级成实现。三处需要明确记录的：

| 技能 | 项目实现版本 | 依据 | 结论 |
| --- | --- | --- | --- |
| 双雄 | 经典版（颜色，非花色）| `SkillDef.description`："与此判定牌**颜色**不同的手牌" | 按颜色实现，未改成花色 |
| 洛神 | ~~经典版（单次判定）~~ **判断错误** | 原依据是 `SkillDef.description`——**这本身就是被污染的一方** | 见 §11：官方文本是"然后你可以重复此流程"，实现与描述一起改正 |
| 龙魂 | 红桃→桃 / 方块→火杀 / 梅花→闪 / 黑桃→无懈 | `SkillDef.description` 与 conversion 表一致 | 一致，未改动 |

---

## 7. 修改的核心通用机制

1. **`src/game/judge_gate.py`（新增）** —— 判定优先闸门，三个状态 + 两个消费点。
2. **`Flow.notify_on_complete`（幂等回调入口）** —— 流程收尾时基类统一触发
   `on_complete`，覆写 `on_settled` 的流程改用同一入口，不会再出现"子类忘了调
   一次，调用方永远等一个已经结束的流程"。
3. **`JudgeFlow.card_recipient`** —— "这张判定牌最终归谁"由发起方声明，
   收尾时直接进手牌；不再需要"先弃后拿"。
4. **`CardConversion.owner_matches` + `for_owner`** —— 需要看拥有者状态的源牌
   谓词（双雄的判定颜色）在技能绑定时折进 `matches`，UI / 引擎 / LAN 读同一份判断。
5. **`ActiveSkillSpec.cost_candidates` / `keep_cards` / `max_cost_cards`** ——
   主动技的合法素材候选、素材不由 activation 代付、规则给出的张数上限。
6. **`CardActionDiscovery.view_as_options` 去重池纳入正常使用** —— 消除
   "界面显示合法、提交被拒"的不一致。

---

## 8. 仍需真人手测的部分

程序化审计覆盖不到、需要人眼确认的：

1. 判定面板的**视觉遮挡**（判定牌是否真的盖住下层所有面板）——
   程序只能断言绘制顺序，观感需要人看。
2. 判定的**节奏**（面板停留时长是否够看清改判）——`MAX_HOLD` 相关的
   体感验收。
3. 多目标技能在 **5 人 / 8 人**桌面的候选点击（程序用 3–4 人构造）。
4. LAN 场景里**两个真人**同时是改判候选时的顺序体验。
5. `audit_p2_view_as` 中"本场景凑不齐素材"的三个技能（龙魂在 2 血时
   需要 2 张同花色；董卓酒池；华佗急救的回合外场合）——规则实现已复核，
   但没构造到"能提交成功"的场景。

---

## 9. 本轮**未**完成的部分

诚实列出，没有含糊：

1. **不是每个技能都有"正反场景"。** 判定类（13 项）、转化类（16 项）、
   真人选牌类（4 项）做了正反 + 取消 + 边界；其余触发技 / 锁定技只做了
   "全量实跑一局 + 静态复核谓词"，没有逐条构造"应该发动 → 确实发动 /
   不该发动 → 确实不能发动"。
2. **限定技 / 觉醒技 / 翻面 / 拼点**没有做专项审计。这三类只有全量跑局覆盖
   （拼点相关的 3 名武将在全量跑局里跑通了，但没有构造"双人拼点 + 改判穿插"
   之类的边界）。
3. **LAN 全链路**只对双雄做了真实 TCP 验证（含 Host 拒绝伪造素材）。
   其余技能的远程路径沿用同一套 `DecisionResponse` + `CardActionDiscovery`
   序列化，但没有逐个跑过。
4. **5 人 / 8 人**桌面的距离与多目标顺序没有专项构造（全量跑局是 4 人）。
5. 静态扫描 `audit_auto_choice.py` 里仍有若干 `target = targets[0]` 形态的
   可疑点，逐条看过都跟在 `ask_targets` 之后（即玩家已经选过），
   属于**误报**，但没有为此写自动化断言。

**因此：当前武将规则审计尚未完整覆盖整个现有武将池。**
未完成清单就是上面 5 条；已完成的 84 名武将见 §4 与 §2 的复现命令。

---

# Phase 16.1 · 规则 Source of Truth 纠偏

> 本节由用户独立核查后发起。它推翻的是**方法**，不只是两条结论：
> Phase 16 把"项目自己的 `SkillDef.description`"当成了版本证据，
> 于是一旦描述本身写错，实现与描述互相印证，审计就给出假 PASS。

## 11.1 审计方法纠偏：Internal SkillDef is not Source of Truth

### 错在哪

Phase 16 判定"本项目实现的是哪一版"时，用的判据是：

```text
先看 SkillDef.description，再判断"实现与描述一致 = 正确"
```

这条链条有一个致命弱点：**描述和实现是同一个人写的**。当描述漏了规则内容时，
实现照着漏掉的描述写，二者"一致"——审计于是判定 PASS。洛神就是这样漏过去的：

| 环节 | Phase 16 的做法 | 实际 |
| --- | --- | --- |
| 版本判断 | 读 `description`："若结果为黑色，你获得此判定牌" | 描述本身漏了"然后你可以重复此流程" |
| 一致性检查 | "实现与描述一致" → PASS | 两边一起错 |
| 结果 | 把"少了一个循环"写成"本项目实现的是经典版" | **给缺陷贴了一个版本标签** |

这正是"规则描述反向污染"：**项目内的描述反向定义了审计基线**，
真正的规则来源被排除在链条之外。

### 现在的判据

`SkillDef` 里的字段**只能**用来回答"这是哪个武将 / 哪个扩展包 / 大致哪个版本候选"：

| 可以用于 | 不可以用于 |
| --- | --- |
| 武将 ID、扩展包、技能名 | 证明该版本的规则内容是什么 |
| 候选版本清单（标准 / 界 / 谋 / SP / OL / 移动版…）| 判断实现"对不对" |
| 定位需要去查哪一条官方文本 | 作为 PASS 的依据 |

Source of Truth 优先级：

1. 三国杀官方武将详情（sanguosha.com）
2. 官方规则 / FAQ / 公告
3. 官方对应版本资料（x.sanguosha.com 等）
4. 可信 Wiki —— **仅作交叉验证**

冲突时的处理顺序：**以确定的官方对应版本为准，同时修三处**——
代码实现、`SkillDef.description`、UI 技能描述（`judge_presentation.rule_text`
一类展示文案）。不允许"因为项目描述和代码一致就判 PASS"。

### 本轮实际抓到的两类问题

| 类型 | 症状 | 例子 |
| --- | --- | --- |
| A 描述错 → 实现跟着错 | 规则内容缺失（少一个分支 / 少一个循环 / 少一个选项）| 洛神（少"可重复"）、刚烈（少"弃两张手牌"）|
| B 描述对但实现简化 | 描述里有"选择一项"，实现直接替他选 | 刚烈（选择权没交出去）|

## 11.2 逐技能官方核实结果

按用户点名逐个联网核实（来源见每行备注）：

| 技能 | 项目描述 | 官方对应版本 | 结论 |
| --- | --- | --- | --- |
| 洛神 | 少"然后你可以重复此流程" | 标准/OL/移动版：可重复 | **冲突 → 已修** |
| 刚烈 | 少"伤害来源选择一项：弃两张手牌 / 受 1 点伤害" | 标准版：伤害来源选择一项 | **冲突 → 已修** |
| 裸衣 | 少摸一张 → 本回合杀/决斗 +1 | 标准版一致 | 一致 |
| 突袭 | 放弃摸牌，获得至多两名其他角色各一张 | 标准版一致 | 一致 |
| 好施 | 多摸两张；>5 张须交一半给手牌最少者 | 标准版一致 | 一致 |
| 恩怨 | 回血对方摸牌 / 受伤须给红桃否则失去 1 体力 | 标准版一致 | 一致 |
| 龙魂 | 同花色 X 张：红桃桃 / 方块火杀 / 梅花闪 / 黑桃无懈 | 一致 | 一致 |
| 武圣 | 红色牌当【杀】 | 一致 | 一致 |
| 龙胆 | 【杀】当【闪】、【闪】当【杀】 | 一致 | 一致 |
| 鬼才 | 判定牌生效前打出一张手牌代替 | 一致 | 一致 |
| 鬼道 | 打出一张黑桃或梅花牌替换 | "黑色牌"（= 黑桃 ∪ 梅花）| 等价 |
| 天妒 | 判定牌生效后可获得 | 一致 | 一致 |
| 铁骑 | 指定目标后判定，红色则不能使用【闪】 | 一致 | 一致 |
| 屯田 | 回合外失去牌 → 判定，非红桃置为「田」 | 一致 | 一致 |
| 双雄 | 颜色（非花色）| 经典版一致 | 一致 |
| 觉醒技（魂姿/凿险/志继/拜印/单骑）| 减 1 点体力上限 + 永久获得技能 | 官方：锁定技 + 限定技，**必须发动** | 一致（实现无确认窗口）|
| 拼点（天义/驱虎/陷阵/烈刃/制霸）| 点数大者赢 | 官方：A 最小、K 最大、**平局双方都没赢**、拼点牌进弃牌堆 | 一致 |
| 强袭 / 神速 / 明策 / 鸡肋 | 描述含"选择一项" | 实现确有选择窗口 | 一致 |
| 限定技（业炎/涅槃/乱武）| 每局限一次 | 官方一致 | 一致 |

**点数与平局**：`mechanics.RANK_VALUES` 为 `A=1 … K=13`，
`PindianResult.initiator_wins` 是 `initiator_value > target_value`
（平局 → 不算赢）✓ 与官方口径一致。

## 11.3 洛神最终实现语义

官方文本（三国杀 OL / 移动版标准甄姬）：

> 准备阶段，你可以进行判定，若结果为黑色，你获得此牌，然后你可以重复此流程。

实现（`LuoshenFlow`）：

```text
询问「是否进行判定」
  ├─ 否 → 流程结束（不写 used、不判定、什么都不动）
  └─ 是 → 判定（走通用 JudgeFlow，改判者每次都能参与）
            ├─ 红色 → 该判定牌进弃牌堆，流程结束
            └─ 黑色 → 判定牌直接进手牌（card_recipient）
                        → 牌堆还有牌？
                             ├─ 没有 → 流程结束
                             └─ 有   → 再问「是否继续判定」→ 回到判定
```

三条硬约束：

1. **每一次判定都走通用 `JudgeFlow`**：鬼才 / 鬼道参与每一次，判定牌去向由
   `card_recipient` 声明，`JudgeGate` 在每次判定期间各自维持最高优先级。
   没有任何"自己写一遍判定"的简化。
2. **重复是玩家的选择**：每一次黑色之后都重新问一次，系统不会自动判到红色为止。
3. **判定牌直取**：黑色牌在判定收尾时直接进手牌，不经弃牌堆，不产生虚假弃牌事件。

## 11.4 洛神验证（10 个场景，30 项断言全通过）

`audit_p2_luoshen.py`：

| # | 场景 | 结果 |
| --- | --- | --- |
| 1 | 第一张黑、第二张红 → 只获得第一张，结束 | PASS |
| 2 | 连续三张黑、第四张红 → 获得前三张（问了 3 次继续）| PASS |
| 3 | 第一张黑之后真人选择停止 → 只获得第一张，不继续 | PASS |
| 4 | 黑色过程中鬼才改成红 → 不获得，流程结束 | PASS |
| 5 | 红色过程中鬼才改成黑 → 获得最终黑牌 + 出现"是否继续" | PASS |
| 6 | 连续黑色过程中每一次判定都占用 JudgeGate | PASS |
| 7 | 远程真人（真实 TCP）自己决定是否继续 | PASS |
| 8 | AI 自行决定继续，不影响本机的选择权 | PASS |
| 9 | 判定牌 ownership 全程唯一（未出现双区域）| PASS |
| 10 | 牌堆耗尽时正常结束，不锁死 | PASS |

## 11.5 顺带修掉的通用缺陷（不是 SkillDef 错误，但属同一类接线问题）

| 缺陷 | 影响 | 修复 |
| --- | --- | --- |
| `ask_option` 的 `(值, 文案)` 形式没有规范化 | 本地二选一按钮与远程下发的 label 都显示成 `('discard', '弃置两张手牌')` 元组字面量 | 规范化：`options` 只存值，文案进 `context["option_labels"]`；本地 / 远程 / AI 三处读同一份 |
| `AIController._respond_option` 假设选项是纯字符串 | AI 提交的是整个元组，技能侧永远读不出自己声明的选项值（刚烈里 AI 一律"选受伤"）| 与上一条同源，规范化后 AI 拿到的是值 |

## 11.6 Phase 16 结论的修正

| 原结论 | 现结论 | 原因 |
| --- | --- | --- |
| 洛神 PASS → FIXED（缺"你可以"、先弃后拿）| **FIXED**，且新增"少可重复"这一条 | Phase 16 的版本判断本身错误 |
| 刚烈 PASS | **FIXED** | 漏掉"伤害来源选择一项"的完整内容 |
| 判定 / 改判专项 17/17 | 仍 17/17，但**洛神一项已作废**，真实覆盖由 Phase 16.1 的 30 项接替 | 该项的 PASS 建立在错误基线上 |

**被推翻的 PASS/FIXED 结论：2 条**（洛神、刚烈）。
其余点名技能经官方文本核实**保持一致**，不因"描述与代码一致"而判定，而是
因"与官方文本一致"而判定。

## 11.7 本轮修改清单

| 文件 | 改动 |
| --- | --- |
| `src/game/skills/standard/wei2.py` | 洛神：`LuoshenFlow` 支持重复判定（每次黑色后重新询问）；description 改为官方文本 |
| `src/game/skills/standard/wei.py` | 刚烈：新增 `GanglieFlow`（伤害来源选择一项）；description 改为官方文本 |
| `src/game/judge_presentation.py` | 洛神 / 刚烈的展示规则文本与结果说明同步官方口径 |
| `src/game/skills/mechanics.py` | `ask_option` 规范化选项值 / 文案；新增 `option_label` |
| `src/game/controllers/human.py` | 二选一面板按钮显示文案而不是元组字面量 |
| `src/game/controllers/remote.py` | 远程下发的选项 label 用文案，value 用真实值 |

---

# Phase 16.2 · 刚烈真正收口

> Phase 16.1 给刚烈补上了"二选一"，但独立复核又发现**两个仍然存在的
> 真人选择权缺陷**——它们都属于同一类："规则要求玩家自己决定，实现替他决定"。

## 12.1 本轮新发现的两个缺陷

| # | 缺陷 | 位置（改动前）| 规则要求 | 实现实际做的 |
| --- | --- | --- | --- | --- |
| 1 | **是否发动刚烈被系统自动决定** | `Ganglie.resolve` 收到伤害直接 `JudgeFlow.start()` | 刚烈不是锁定技，"**你可以**进行判定"——由夏侯惇本人决定 | 受伤即判定，夏侯惇没有拒绝的机会 |
| 2 | **弃哪两张由程序取前两张** | `GanglieFlow._discard` 的 `list(self.source.hand)[:2]` | 伤害来源**自己指定**弃置哪两张手牌 | 固定丢手牌列表的前两张，双方都能预测 |

第 2 条与 Phase 16 已修的"双雄 / 眩惑 / 明策 / 直谏 / 举荐"是同一个病灶：
**规则要求真人指定具体牌，实现替他取前 N 张。**

此外还有两个由本轮暴露出来的**接线缺陷**（不是 SkillDef 错误）：

| # | 缺陷 | 影响 |
| --- | --- | --- |
| 3 | `SELECT_CARDS` 引擎校验不查重复 | 同一个牌对象可以被当成"两张"凑数（本地 / 脚本路径），弃牌时同一张牌被移动两次 |
| 4 | 远程控制器按**索引**解析选项，而下发的是**值** | 远程真人任何"选择一项"都会被判"选项不是数字"直接拒绝（刚烈在联机下根本走不到选牌）|

第 4 条是 Phase 16.1 引入的不一致：那一轮把"下发值 + 文案"接上时，只改了
下发侧，回传侧的解析没跟着改。

## 12.2 实现

刚烈现在是一条**完整的 Flow**（`GanglieFlow`），四个窗口的次序与选择权
全部是流程自己的状态——不再依赖 `Skill` 实例上的临时字段（`_judge_source`
那种字段在"取消"或"同一回合连续两次受伤"时会互相污染）：

```text
受到伤害
  → 询问夏侯惇「是否发动【刚烈】」（CONFIRM，target = 夏侯惇）
       ├─ 否 → 什么都不发生（不写状态、不抽判定牌、不产生 JudgeGate、不写日志）
       └─ 是 → 通用 JudgeFlow（鬼才 / 鬼道 / JudgeGate 照常参与）
                 ├─ 最终生效判定牌为**红桃** → 结束，不惩罚
                 └─ 非红桃 → 伤害来源选择一项（CHOOSE_OPTION，target = 伤害来源）
                              ├─ 手牌 < 2 张：没有这个选项，直接只能受伤
                              ├─ 选「受到 1 点伤害」→ 由夏侯惇作为来源造成 1 点
                              └─ 选「弃置两张手牌」→ SELECT_CARDS
                                                        （min = max = 2，
                                                         候选 = 他的手牌）
                                                        → 选满两张后才一起进弃牌堆
```

三条硬约束：

1. **判定仍走统一 `JudgeFlow`**：鬼才 / 鬼道 / JudgeGate / card ownership
   全部由它负责；判据是**最终生效判定牌**的花色，不是颜色。
2. **不提前支付**：进入选牌窗口到"选满两张并提交"之间，一张牌都不动。
   窗口不支持取消——官方没有"选了弃牌再退回受伤"这种撤销能力，不凭空加。
3. **手牌不足两张不摆假的二选一**（官方 FAQ：此时只能承受伤害）。

## 12.3 验证（15 个场景，37 项断言全通过）

`audit_p2_ganglie.py`：

| # | 场景 | 结果 |
| --- | --- | --- |
| 1 | 真人选择不发动 → 不判定、不写状态、无闸门、无请求残留 | PASS |
| 2 | 发动 → 红桃 → 什么都不发生 | PASS |
| 3 | 非红桃 + 1 张手牌 → 不摆假的二选一，直接受伤 | PASS |
| 4 | 非红桃 + 正好 2 张 → 选弃牌 → 两张全弃 | PASS |
| 5 | 非红桃 + 4 张 → 真人**自己指定任意两张**，未选的两张仍在手里 | PASS |
| 6 | 刚烈代码里没有 `hand[:N]` / `hand[0]`，弃牌走 `SELECT_CARDS` | PASS |
| 7 | 选「受到伤害」→ 由夏侯惇作为伤害来源造成 1 点 | PASS |
| 8 | 初始红桃 → 鬼才改成黑桃 → 进入惩罚 | PASS |
| 9 | 初始黑桃 → 鬼才改成红桃 → 不惩罚 | PASS |
| 10 | Local 夏侯惇自己决定是否发动 | PASS |
| 11 | Remote：本地夏侯惇自己决定是否发动 | PASS |
| 12/13 | Remote 伤害来源自己选一项、自己挑两张（真实 TCP）| PASS |
| 14 | 恶意提交：非自己手牌 / 重复 id / 只提交一张 / 提交三张 —— 全部被拒且状态不变 | PASS |
| 15 | 整条流程里没有一张牌同时属于两个区域 | PASS |

## 12.4 针对性扫描（不是重跑 154 技能）

`audit_choice_scan.py` 扫描 `[:1] / [:2] / [:3] / hand[0] / sorted(...)[0] /
random.choice` 在技能结算路径上的出现（AI 策略层豁免），命中 5 处：

| 位置 | 判断 | 处理 |
| --- | --- | --- |
| `fire.py` 乱击 `_luanji_same_suit_pair` + `_activate_luanji` | **真人路径替玩家挑**：没传就自动找一对同花色的牌打出去 | **已修**：改为 `cost_cards=2 / keep_cards=True`，玩家自己选两张，技能只复核"数量够 + 花色相同" |
| `yijiang.py` 举荐 `list(cards or ())[:3]` | 玩家已选牌之后的**上限截断**（引擎另有 `max_cost_cards=3` 校验）| 保留 |
| `wei.py` 反馈 `source.hand[0]` | 从**看不见的**手牌里取一张：规则上玩家不指定具体哪张，但固定取首元素可被记牌预测 | **已修**：改为按对局随机源抽一张 |
| `wei.py` 突袭 `target.hand[0]` | 同上 | **已修**：同上 |

**新扫描又发现真人自动选牌 1 处（乱击）**，另有 2 处"暗牌固定取首元素"
（反馈 / 突袭）属可预测性问题，一并按官方口径改成随机。

## 12.5 Phase 16.2 对结论的影响

| 原结论 | 现结论 | 原因 |
| --- | --- | --- |
| 刚烈（Phase 16.1）FIXED | **仍 FIXED，但当时并未修完**：发动权与"弃哪两张"两处仍是程序决定 | 本轮补齐 |
| 判定 / 改判专项 17/17 | 仍 17/17；其中刚烈那一项的断言已改为"产生了惩罚"（两条合法路径之一），细节由 `audit_p2_ganglie` 覆盖 | 语义变了，断言跟着变 |
| 乱击 | **新发现 FIXED**（补进 §5 的统计）| 本轮的针对性扫描 |

**逐技能 Bug 统计更新**：错误类型表新增"真人选牌权被替代 +1（乱击）"，
"规则内容缺失 +2（刚烈发动权、刚烈弃牌选择）"。

## 12.6 本轮修改清单

| 文件 | 改动 |
| --- | --- |
| `src/game/skills/standard/wei.py` | 刚烈重写为完整 `GanglieFlow`（是否发动 → 判定 → 二选一 → 选两张）；删除 `Skill` 上的临时字段；反馈改为随机取暗牌；新增 `_random_hand_card` |
| `src/game/skills/expansions/fire.py` | 乱击：删掉"没传就自动找同花色对"，改为 `cost_cards=2 / keep_cards=True` + 复核 |
| `src/game/skills/activation.py` | 技能发动失败时保留技能自己写的提示（"两张牌的花色必须相同"）|
| `src/game/engine/runtime.py` | `SELECT_CARDS` 增加**重复牌**校验（与选目标的查重对称）|
| `src/game/controllers/remote.py` | 选项按**值**解析（兼容数字索引），与下发的"value + label"对齐 |
