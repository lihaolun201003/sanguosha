# 三国杀项目美术素材审计与整理报告 v01

- 报告日期：2026-09-20
- 原始素材目录：`C:\Users\lihao\Desktop\sanguosha\sucai`
- 项目素材目录：`C:\Users\lihao\Desktop\sanguosha\sanguosha -zcode\assets`
- 本次性质：**只读取、只识别、只分类、只复制**。未接入游戏，未改动任何代码。
- 机器可读索引：`assets/asset_inventory.json`

---

## 1. 原始素材目录概况

`sucai` 是一套结构清晰的**卡牌图包**，共 10 个目录、82 个文件：

```
sucai/
├─ 25 个武将牌（直接放在顶层，文件名即武将名）
├─ 将牌.png                    武将牌背面
├─ EX/          4 张           带 EX 扩展标记的标准版补充牌
├─ 军争/        10 张          军争篇扩展牌
├─ 标准/
│  ├─ 基本牌/   3 张
│  ├─ 武器/     8 张
│  ├─ 锦囊/     12 张
│  ├─ 防具/     1 张
│  └─ 马/       6 张
└─ 身份/        4 张           主公 / 忠臣 / 反贼 / 内奸
```

每个目录内含一个 `Thumbs.db`（Windows 资源管理器缩略图缓存），共 8 个，**不是游戏素材**，未复制。

## 2. 关键结论：这批素材的形态

**这是一整套《三国杀》标准版 + 军争篇 + EX 的「完整卡面扫描图」，不是拆好的 UI 素材包。**

对 6 张代表性图片做了实际预览确认：

| 预览样本 | 实际内容 |
| --- | --- |
| `关羽.png` | 完整武将牌：势力篆字「蜀」、体力 XXXX、立绘、技能「武圣」及描述、编号 `SHU 002` |
| `将牌.png` | 武将牌背面：黑金边框 + 中央书法「将」字 + 祥云纹 |
| `身份/主公.png` | 完整身份牌：红黑配色 + 「主公」印章式构图 |
| `标准/基本牌/杀.png` | 完整游戏牌正面（卡框 + 牌名 + 花色 + 效果文字） |
| `标准/武器/诸葛连弩.png` | 完整装备牌：攻击范围 1 + 「装备/武器」标记 |
| `标准/马/的卢.png` | 完整 +1 马牌：「装备/马」标记 |

推论（直接影响后续接入策略）：

- **每张图片 = 一整张牌**，牌框、牌名、花色、效果文字、插画全部烤在同一张图里。
- **没有**独立的武将头像、半身立绘、技能图标、势力图标、花色图标、血量图标。
- **没有**按钮、面板、边框、选中框、提示框、Logo、开始界面等任何 UI 组件。
- 因此这批素材在接入时，只能整张显示，或者在后续「素材适配阶段」从卡面上裁切抠图。

## 3. 数量统计

| 项目 | 数量 | 说明 |
| --- | --- | --- |
| 原目录总文件数 | 82 | 74 张 PNG + 8 个 Thumbs.db |
| 图片 | **74** | 全部为 PNG，无 jpg/webp/bmp/gif |
| 武将素材 | **26** | 25 张武将牌正面 + 1 张武将牌背面 |
| 卡牌素材 | **44** | 标准版 29 + EX 4 + 军争 10 + 1 张闪电插画局部 |
| Identity（身份牌） | **4** | 主公 / 忠臣 / 反贼 / 内奸 |
| UI 素材 | **0** | 本图包中不存在 UI 组件 |
| 音频 | **0** | 无 mp3/wav/ogg/flac |
| 字体 | **0** | 无 ttf/otf |
| 压缩包 | **0** | 无 zip/rar/7z，无需解压 |
| Unknown | **0** | 全部素材均成功归类，无一落入 unknown |
| 完全重复资源 | **0** | 74 张图的 MD5 两两不同，无内容完全相同的文件 |
| 复制总容量 | 23.6 MB | 最大 423 KB，最小 173 KB |

> 计数方法：文件按扩展名与所属目录归类；重复检测为逐文件 MD5 比对；透明背景为解码 PNG 像素后统计 alpha 通道。

## 4. 图片规格分布

| 尺寸 | 张数 | 对应素材 |
| --- | --- | --- |
| 420×572 | 51 | 武将牌、武将牌背、标准版基本牌/武器/防具/马、身份牌、EX 牌 |
| 320×436 | 18 | 军争篇各牌、标准版竖版锦囊 |
| 436×320 | 2 | `乐不思蜀`、`兵粮寸断`（延时锦囊的横置形态） |
| 400×562 | 2 | `无懈可击`、`绝影`（尺寸偏小，疑似不同批次） |
| 337×240 | 1 | `闪电` 横向插画局部 |

色彩与透明：

| 透明情况 | 张数 | 说明 |
| --- | --- | --- |
| 透明背景（RGBA） | 71 | 卡牌轮廓外为全透明，可直接叠放到任意背景；透明像素占比多在 6%～11% |
| 大半不透明，仅有 alpha 通道 | 1 | `cards/misc/闪电.png`，透明像素仅 1.45% |
| 近乎不透明，仅有 alpha 通道 | 1 | `cards/standard/horses/绝影.png`，透明像素仅 0.08%（只在四角圆角处） |
| 完全不透明 | 1 | `cards/standard/tricks/无懈可击.png`（color_type=2 真彩色，根本没有 alpha 通道） |

> 71 张透明背景的卡牌图对 Pygame 很友好，缩放后不会带白底。`绝影` 与 `无懈可击` 属例外，接入时需注意可能带不透明底板。

## 5. 武将素材情况

### 5.1 当前第一批 11 名正式武将：**全部命中，无缺口**

素材目录中 25 个武将名与项目正式武将的比对结果：

| 正式武将 | 素材文件 | 状态 |
| --- | --- | --- |
| 张飞 | `generals/cards/张飞.png` | ✅ |
| 黄月英 | `generals/cards/黄月英.png` | ✅ |
| 夏侯惇 | `generals/cards/夏侯惇.png` | ✅ |
| 曹操 | `generals/cards/曹操.png` | ✅ |
| 司马懿 | `generals/cards/司马懿.png` | ✅ |
| 郭嘉 | `generals/cards/郭嘉.png` | ✅ |
| 张辽 | `generals/cards/张辽.png` | ✅ |
| 关羽 | `generals/cards/关羽.png` | ✅ |
| 赵云 | `generals/cards/赵云.png` | ✅ |
| 周瑜 | `generals/cards/周瑜.png` | ✅ |
| 孙尚香 | `generals/cards/孙尚香.png` | ✅ |

每名武将**只有 1 张素材**（一张完整武将牌），没有多版本头像可选。

### 5.2 额外 14 名武将（本次未接入，已分类保留）

`刘备`、`华佗`、`吕布`、`吕蒙`、`大乔`、`孙权`、`甄姬`、`甘宁`、`许诸`、`诸葛亮`、`貂蝉`、`陆逊`、`马超`、`黄盖`

这 14 名与 11 名正式武将放在同一目录 `assets/generals/cards/`，命名一致，后续扩充武将池时可直接取用。

### 5.3 武将牌背面

`将牌.png` → `assets/generals/back/将牌.png`，可用于「暗置武将」「未翻开的武将牌」显示。

## 6. 卡牌素材情况

### 6.1 标准版（29 张）

| 子类 | 张数 | 牌名 |
| --- | --- | --- |
| 基本牌 | 3 | 杀、闪、桃 |
| 锦囊（即时） | 10 | 无中生有、过河拆桥、顺手牵羊、决斗、南蛮入侵、万箭齐发、桃园结义、五谷丰登、无懈可击、借刀杀人 |
| 锦囊（延时） | 1 | 乐不思蜀（横版） |
| 锦囊（延时） | 1 | 闪电 |
| 武器 | 8 | 诸葛连弩、雌雄双剑、青钢剑、青龙偃月刀、丈八蛇矛、贯石斧、方天画戟、麒麟弓 |
| 防具 | 1 | 八卦阵 |
| 马 | 6 | 赤兔、大宛、紫骍、绝影、的卢、爪黄飞电 |

> 项目牌堆里已有的其他锦囊（如「闪电」判定、「乐不思蜀」判定）逻辑都能对应上素材；`顺手牵羊`、`过河拆桥`、`无中生有` 等均在同一目录内。

### 6.2 EX 扩展（4 张）

`仁王盾`、`寒冰剑`、`无懈`、`闪电`

- `仁王盾`、`寒冰剑` 是带 **EX 扩展标记**的标准版装备牌。
- `无懈`、`闪电` 是标准版同名牌的 **EX 版重印**，与标准目录下的同名牌是**两个不同版本**（见第 8 节）。

### 6.3 军争篇（10 张）

| 类别 | 牌名 |
| --- | --- |
| 基本牌 | 火杀、雷杀、酒 |
| 锦囊 | 火攻、铁索连环 |
| 延时锦囊 | 兵粮寸断（横版 436×320） |
| 武器 | 古锭刀、朱雀羽扇 |
| 防具 | 白银狮子、藤甲 |

> 项目已有「杀/火杀/雷杀/闪/桃/酒」以及大量锦囊与装备逻辑，这批素材能一一对应，但**本次不做任何接入**。

### 6.4 孤例素材（1 张）

`标准/锦囊/闪电.png`（337×240）不是完整卡面，画面只有闪电插画 + 「闪电」牌名标签 + 花色标记，**疑似从卡面裁出的图案素材**。已放入 `assets/cards/misc/`，未强行并入锦囊目录。

## 7. Identity 与 UI 素材情况

**Identity（4 张）**：`身份/主公.png`、`内奸.png`、`反贼.png`、`忠臣.png` → `assets/identities/`
尺寸均为 420×572、透明背景，是完整的身份牌图（不是小图标），可直接用于身份揭示与身份展示。

**UI 素材：0 张。** 素材包中完全不存在以下任何一类：

```
按钮 / 面板背景 / 桌面背景 / 牌桌素材 / UI 边框 / 提示框 / 选中框 / hover 框
技能图标 / 势力图标 / 花色图标 / 体力图标 / 特效 / 动画帧 / 游戏 Logo / 开始界面
```

这意味着：当前项目的 Pygame UI 仍需继续用代码绘制的方案；想要贴图化 UI，需要另找素材。

## 8. 多版本与重复情况

### 8.1 同一张牌的两个版本（已全部保留，未删除任何一版）

| 牌 | 版本 A | 版本 B | 差异 |
| --- | --- | --- | --- |
| 无懈可击 | `cards/ex/无懈.png`（420×572，RGBA 透明） | `cards/standard/tricks/无懈可击.png`（400×562，真彩色不透明底） | 尺寸、透明性、画风批次均不同 |
| 闪电 | `cards/ex/闪电.png`（420×572，完整卡面） | `cards/misc/闪电.png`（337×240，横向插画局部） | 一个是完整卡面，一个是图案局部 |

### 8.2 尺寸/批次差异

`标准/马/绝影.png`（400×562，几乎不透明）与本组其余 5 张马牌（420×572，透明背景）规格不同，**疑似不同来源或不同批次**，已保留原样并在索引 `notes` 中标注。

### 8.3 完全重复资源

**0 组。** 74 张图的 MD5 互不相同，没有任何两张文件内容完全一致。

### 8.4 同名文件

- `Thumbs.db` 在 8 个目录中同名（系统缓存，未复制）。
- `闪电.png` 在 `EX/` 与 `标准/锦囊/` 两处同名但内容不同 —— 复制后分别位于 `cards/ex/` 与 `cards/misc/`，**未发生覆盖，也没有被改名**。

## 9. unknown 情况

**unknown 数量为 0。** 全部 74 张图都按「原目录语义 + 文件名 + 尺寸 + 实际预览」明确归类，没有任何资源因为无法判断而落到 `assets/unknown/`。

唯一接近「需说明」的是 `标准/锦囊/闪电.png`（插画局部），已单独放入 `cards/misc/` 并在索引中写明它是什么。

## 10. 最终复制去向与目录结构

所有素材以 **COPY（复制）** 方式写入，原目录文件保持原样。

```
assets/
├─ asset_inventory.json                机器可读索引（74 条）
├─ generals/
│  ├─ cards/        25 张武将牌正面
│  └─ back/          1 张武将牌背面（将牌.png）
├─ cards/
│  ├─ standard/
│  │  ├─ basic/      3 张（杀、闪、桃）
│  │  ├─ tricks/    11 张（标准版即时+延时锦囊）
│  │  ├─ weapons/    8 张
│  │  ├─ armor/      1 张（八卦阵）
│  │  └─ horses/     6 张
│  ├─ ex/            4 张（EX 扩展牌）
│  ├─ junzheng/     10 张（军争篇）
│  └─ misc/          1 张（闪电插画局部）
└─ identities/       4 张（主公/忠臣/反贼/内奸）
```

设计取舍：

- **保留原资源包语义**：标准版、军争篇、EX 三组牌各自成目录，没有打散合并进 `basic/tricks/equipment` 大池子，避免丢失「这批牌同属一个扩展」的对应关系。
- **文件名全部保留原中文名**，未做任何重命名（`关羽.png`、`杀.png`、`铁索连环.png` 等原样保留）。
- **未创建 `audio/`、`fonts/`、`ui/`、`effects/` 空目录**，因为素材包中确实没有这类资源，避免产生误导性的空壳结构。
- **武将牌没有拆到 `generals/portraits/`（头像）**，因为素材本身是完整武将牌，拆解属于后续素材加工阶段的工作。

## 11. 潜在命名冲突

- 复制过程中冲突数：**0**。
- 目标 `assets/` 在本次操作前**完全为空**，不存在覆盖已有项目资源的可能。
- 74 个目标文件名与源文件名一一对应，无 `__v2` 之类的重命名。
- 风险提示：中文文件名在 Windows + Pygame 下可正常读取（项目已有中文内容），但未来接入时建议通过资源 ID 映射表引用，避免代码里散落中文字符串。

## 12. 来源 / 许可说明

**未在素材目录中发现明确来源/许可说明。** 具体核查结果：

- `sucai` 及其所有子目录中**没有** `README`、`LICENSE`、`来源说明`、`作者说明` 或任何 `.txt`/`.md`/`.url`/`.html` 说明文件。
- 素材包中**没有压缩包**，因此不存在「解压后才有说明」的情况。
- 从实际预览的卡面上可见印刷信息：`© 2008 游卡桌游`（版权署名）与插画师署名（如关羽牌的 `Illustration Kayak`）。这些是**卡面自带的印刷文字**，不是素材目录里的独立说明文件。

本报告不对版权状态做任何推测或判断，仅如实记录以上观察结果。

## 13. Priority Assets（后续接入优先级建议）

按「接入收益 / 接入难度」排序，**本次一律不接入**，仅供后续阶段参考：

| 优先级 | 素材 | 理由 |
| --- | --- | --- |
| 1 | 武将牌正面（尤其 11 名正式武将，25 张全量） | 当前项目已有武将选择与武将系统，接入后武将显示立刻从代码绘制变为真实卡面 |
| 2 | 武将牌背面 `将牌.png` | 1 张图即可支撑「暗置武将」显示，成本极低 |
| 3 | 标准版卡牌正面（29 张） | 手牌与装备区是玩家看得最久的区域，透明背景已就绪，缩放即可用 |
| 4 | 身份牌（4 张） | 身份模式已有完整逻辑，接入后身份揭示界面可直接上图 |
| 5 | 军争篇 + EX（14 张） | 扩展牌在牌堆中已存在，可与标准版一并接入 |
| 6 | `cards/misc/闪电.png` 插画局部 | 可用于判定区「闪电」标记的小图标 |

**明确缺口（这批素材无法满足，需要另找）**：桌面/牌桌背景、按钮与面板、势力与体力图标、技能图标、卡牌背面（游戏牌通用背面）、音效、BGM、字体。

> 注意：素材中**只有武将牌背面**，**没有普通游戏牌的背面**。如果后续要做「牌堆/摸牌堆」显示，需要另行准备。

## 14. 后续接入建议

1. **先建资源 ID 映射表**：不要在任何 `.py` 里直接硬编码 `"关羽.png"` 这类中文路径。建议后续新增一个资源清单（如 `assets` 索引对应的映射模块），用英文 ID（`general_guanyu`、`card_sha`）指向实际文件。
2. **缩放策略单独定**：素材是 420×572 级别的卡面图，直接按原始尺寸绘制会超出屏幕。建议在接入阶段统一做一次 `pygame.transform.smoothscale` 到 UI 目标尺寸并缓存（属于渲染层改动，本次不做）。透明背景的 71 张图缩放后无白边，可直接使用。
3. **兜底逻辑**：接入时应保留「图片缺失 → 回退到现有代码绘制卡面」的路径，避免某一版素材不全导致游戏无法运行。素材中 `无懈可击` 与 `绝影` 尺寸异常，最好先确认它们在目标 UI 尺寸下是否清晰。
4. **多版本先都留着**：`无懈可击`、`闪电` 的两个版本暂不做取舍，接入时通过映射表选定其中一个，另一个保留在 `assets` 中备用。
5. **武将扩充时机**：额外 14 名武将的素材已就位且命名规范，后续扩展武将池时无需再找图，直接从 `assets/generals/cards/` 取用即可；但要注意这些武将是**标准版其余武将**，技能逻辑需要单独实现。

## 15. 本次验收清单

| 检查项 | 结果 |
| --- | --- |
| 完整扫描 sucai（递归，含所有子目录） | ✅ 82/82 文件 |
| 原 sucai 完全未修改 | ✅ 82 个文件 MD5 与操作前完全一致 |
| 识别主要素材类别 | ✅ 11 个 category |
| 建立项目 assets/ | ✅ |
| 素材已复制而非移动 | ✅ 使用 copy2，原文件原地保留 |
| 无已有项目素材被覆盖 | ✅ assets 操作前为空，冲突数 0 |
| 武将素材已分类 | ✅ 25 正面 + 1 背面 |
| 卡牌素材已分类 | ✅ 标准/EX/军争/misc |
| UI / Identity 素材已分类 | ✅ Identity 4 张；UI 素材确认不存在 |
| 音频/字体如存在已分类 | ✅ 确认不存在 |
| 不确定资源保留到 unknown | ✅ 无不确定资源，unknown 为空 |
| 多版本资源未被擅自删除 | ✅ 无懈可击、闪电两版均保留 |
| 完全重复资源未擅自删除 | ✅ 无重复；同名文件分组存放 |
| 生成 asset_inventory.json | ✅ `assets/asset_inventory.json` |
| 生成 assets_inventory_v01.md | ✅ 本文件 |
| 未修改任何游戏逻辑 / Renderer / UI / tests | ✅ 代码文件哈希与操作前完全一致 |
| 最终检查代码 diff 干净 | ✅ 见第 16 节 |

## 16. 未修改代码的验证

- 操作前对项目内全部 `.py` / `.json` / `.yaml` / `.toml` / `.ini` / `.md` 文件记录了 MD5 基线（171 个文件）。
- 操作后重新比对：**无任何代码文件内容发生变化**。
- 本次新增文件仅位于：`assets/**`（74 张图 + `asset_inventory.json`）与 `docs/reports/assets_inventory_v01.md`。
- `src/`、`main.py`、`tests/`、`tools/` 下的文件均未触碰（工作区中原有的未提交改动为本次任务之前就存在的状态，非本次产生）。
